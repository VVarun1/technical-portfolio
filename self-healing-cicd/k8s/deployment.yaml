apiVersion: apps/v1
kind: Deployment
metadata:
  name: sample-app
spec:
  strategy:
    type: RollingUpdate
  template:
    spec:
      containers:
      - name: app
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
